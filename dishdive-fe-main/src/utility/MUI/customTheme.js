import { createTheme, withTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#00B87D',
    },
    background: {
      default: withTheme,
    },
  },
});
