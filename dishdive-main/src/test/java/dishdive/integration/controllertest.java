/*
package s3.indv.dishdive.integration;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.http.ResponseEntity;
import org.springframework.test.web.servlet.MockMvc;
import s3.indv.dishdive.user.controller.UserController;
import s3.indv.dishdive.user.repository.UserRepository;

*/
/*@SpringBootTest
@AutoConfigureMockMvc
public class controllertest {
    @Autowired
    private MockMvc web;

    @Test
    public void getsAll() throw Exception{
        web.perform((get("osmething")))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.length()").value(0));
    }

    @Test
    public void getallrandom(){
        UserRepository repo = mock(UserRepository.class);

        UserController controller = new UserController(repo);

        ResponseEntity<?> response = controller.getAll();



        assert //something;*//*

    }
}
*/
